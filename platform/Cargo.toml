[package]
name = "platform"
version = "0.1.0"
edition = "2024"

[dependencies]
kernel = { path = "../kernel" }
serde = { version = "1", features = ["derive"] }
thiserror = "1"
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
toml = "0.8"